// Plan-compliant test file location.
// The runnable SolidJS component test lives in tests/browser/IllustTags.browser.test.tsx
// because the unit vitest.config.ts uses a node environment without DOM/Solid support.
export {};
